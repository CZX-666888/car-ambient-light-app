# 汽车氛围灯 APP 开发说明书

**版本**：V1.0

**日期**：2026-09-07

**状态**：初稿（待评审）

**参考原型**：微信小程序 "汽车氛围灯"（功能与界面 1:1 还原）



***

## 目录



1. [项目概述](#1-项目概述)

2. [功能需求详解](#2-功能需求详解)

3. [界面设计规范](#3-界面设计规范)

4. [交互流程设计](#4-交互流程设计)

5. [技术架构设计](#5-技术架构设计)

6. [蓝牙通信协议](#6-蓝牙通信协议)

7. [数据模型定义](#7-数据模型定义)

8. [开发计划与里程碑](#8-开发计划与里程碑)

9. [验收标准](#9-验收标准)

10. [附录](#10-附录)



***

## 1. 项目概述

### 1.1 项目背景

基于现有微信小程序 "汽车氛围灯" 的成熟功能与交互设计，开发独立原生 APP，实现对车载 LED 氛围灯硬件的蓝牙无线控制。APP 需在功能完整性、界面视觉、交互体验上与小程序保持一致，同时利用原生平台能力提供更稳定的蓝牙连接和更流畅的操作体验。

### 1.2 项目目标



| 目标   | 说明                              |
| ---- | ------------------------------- |
| 功能还原 | 100% 还原小程序已有功能（灯光控制、蓝牙连接、分区管理等） |
| 界面一致 | 视觉风格、布局结构、交互逻辑与小程序保持一致          |
| 原生体验 | 利用原生蓝牙 API 提升连接稳定性和响应速度         |
| 跨平台  | 优先 Android，后续可扩展 iOS            |

### 1.3 适用范围



* **硬件**：车载 BLE 蓝牙氛围灯（设备名格式：LED-XXXXXXXX）

* **平台**：Android 8.0+（API Level 26+），后续支持 iOS 13+

* **用户**：车载氛围灯设备拥有者

### 1.4 术语定义



| 术语   | 说明                         |
| ---- | -------------------------- |
| BLE  | Bluetooth Low Energy，蓝牙低功耗 |
| 分区   | 车内可独立控制的灯光区域（共 7 路）        |
| 色盘   | 环形全色域颜色选择组件                |
| 动态效果 | 灯光的动态变化模式（呼吸 / 跳变）         |
| 颜色模式 | 颜色的显示方式（单色 / 十二色 / 七色）     |



***

## 2. 功能需求详解

### 2.1 功能模块总览



```
汽车氛围灯 APP

├── 2.2 蓝牙连接模块

│   ├── 蓝牙开关检测

│   ├── 设备扫描

│   ├── 设备连接/断开

│   ├── 连接状态监听

│   └── 自动重连

├── 2.3 灯光控制模块（核心）

│   ├── 分区选择

│   ├── 颜色选择（色盘 + 预设色）

│   ├── 亮度调节

│   ├── 速度调节

│   ├── 颜色模式切换

│   └── 动态效果切换

├── 2.4 辅助功能模块

│   ├── 帮助中心

│   └── 关于我们

└── 2.5 系统功能

&#x20;   ├── 权限管理（蓝牙/位置）

&#x20;   ├── 后台保活

&#x20;   └── 错误处理与提示
```

### 2.2 蓝牙连接模块

#### 2.2.1 蓝牙开关检测



* APP 启动时检测系统蓝牙状态

* 蓝牙未开启时，弹出系统级蓝牙开启请求

* 蓝牙状态变化时实时监听并更新 UI

#### 2.2.2 设备扫描



| 项目   | 说明                                                                                                   |
| ---- | ---------------------------------------------------------------------------------------------------- |
| 扫描触发 | 进入蓝牙设置页自动开始扫描；点击 "点击重新搜索" 手动触发                                                                       |
| 扫描过滤 | **固定前缀过滤**：仅显示蓝牙广播名称（BluetoothDevice.name）以 `"LED-"` 开头的 BLE 设备，其他所有设备（名称不符、名称为空、无名称）一律过滤掉，不展示在设备列表中 |
| 扫描超时 | 10 秒（可配置），超时后自动停止                                                                                    |
| 结果展示 | 设备名称 + 连接状态指示（绿色圆点 = 已连接，灰色 = 未连接）                                                                   |
| 重复设备 | 同一设备只显示一次，按信号强度（RSSI）从强到弱排序                                                                          |

**扫描过滤技术实现要求：**



1. **过滤规则**：严格前缀匹配，设备名称必须以 `"LED-"`（4 个字符：L、E、D、连字符）开头，非 "包含" 匹配

* 正确示例：`LED-2202080`、`LED-ABC123`、`LED-1`

* 错误示例（需过滤）：`LED2202080`（无连字符）、`MyLED-123`（前缀不符）、`led-123`（大小写敏感，需全大写 LED）、名称为空或 null 的设备

1. **双重过滤机制**：

* **第一层（硬件过滤）**：使用 Android `ScanFilter` 设置 `setDeviceName("LED-")` 前缀过滤，由系统蓝牙芯片在硬件层面过滤，降低 CPU 开销和功耗

* **第二层（软件过滤）**：在 `ScanCallback.onScanResult()` 回调中再次校验 `device.name?.startsWith("LED-") == true`，防止部分设备 / 芯片不支持硬件前缀过滤导致漏网，确保过滤万无一失

1. **过滤逻辑伪代码**：



```
// 构建扫描过滤器

val scanFilter = ScanFilter.Builder()

&#x20;   .setDeviceName("LED-")  // 硬件层前缀过滤

&#x20;   .build()

// 扫描结果回调中的软件二次校验

override fun onScanResult(callbackType: Int, result: ScanResult) {

&#x20;   val device = result.device

&#x20;   val name = device.name

&#x20;   // 软件层二次校验：名称非空且严格以 "LED-" 开头

&#x20;   if (name != null && name.startsWith("LED-")) {

&#x20;       // 加入设备列表，更新 UI

&#x20;       addDevice(device, result.rssi)

&#x20;   }

&#x20;   // 名称为空或前缀不符的设备，直接丢弃，不展示

}
```



1. **大小写处理**：设备名称前缀必须为大写 `"LED-"`，小写 `"led-"` 不匹配；若硬件设备可能存在小写命名，可在软件层增加 `name.toUpperCase().startsWith("LED-")` 兼容处理（需根据实际硬件命名确认）

#### 2.2.3 设备连接 / 断开



* 点击未连接设备 → 发起连接 → 显示 "连接中" 加载动画

* 连接成功 → 弹出 "蓝牙连接成功"Toast（2 秒），设备状态变绿

* 连接失败 → 弹出 "连接失败，请重试" 提示，保留在设备列表

* 已连接设备点击 → 弹出确认对话框 "是否断开连接？"

* 断开成功 → 设备状态变灰，主控制页提示 "设备已断开"

#### 2.2.4 连接状态监听



* 全局监听蓝牙连接状态变化

* 连接意外断开时：


  * 弹出 "蓝牙连接已断开" 提示

  * 自动尝试重连（最多 3 次，间隔 2 秒）

  * 重连失败后提示 "连接已断开，请重新连接"

#### 2.2.5 自动重连



* APP 下次启动时，若之前有成功连接的设备，自动尝试连接

* 已连接设备 MAC 地址持久化存储

* 自动重连超时 5 秒，失败后进入设备列表等待手动选择

### 2.3 灯光控制模块（核心）

#### 2.3.1 分区选择

**分区定义（共 7 路 + 全部）：**



| 分区 ID | 分区名称 | 说明           |
| ----- | ---- | ------------ |
| 0x00  | 全部   | 同时控制所有分区     |
| 0x01  | 前部   | 车前部仪表台氛围灯    |
| 0x02  | 车门   | 四车门氛围灯（整体控制） |
| 0x03  | 脚部   | 脚部空间氛围灯      |
| 0x04  | 顶部   | 车顶 / 天窗周边氛围灯 |
| 0x05  | 车门一区 | 左侧车门氛围灯      |
| 0x06  | 车门二区 | 右侧车门氛围灯      |

**交互方式：**



* 主控制页底部左侧按钮显示当前选中分区（默认 "全部"）

* 点击按钮 → 从底部弹出位置选择弹窗

* 弹窗内为可滚动列表，单选模式

* 选中项高亮显示

* 点击 "确定"→ 生效并关闭弹窗，按钮文字更新

* 点击 "取消"→ 不生效并关闭弹窗

* 切换分区后，色盘 / 颜色 / 模式 / 效果状态保持（各分区独立记忆）

#### 2.3.2 颜色选择（三层环形色盘组件）

色盘组件为**三层同心圆环**结构，由外到内分别为：亮度调节环、颜色选择环、当前颜色显示圆。



```
&#x20;       ╭─────────────────────────╮

&#x20;      │  第1层：亮度调节环（最外圈）  │  ← 细小竖线/刻度组成的环，拖动调节亮度

&#x20;      │  ╭─────────────────────╮  │

&#x20;      │  │  第2层：颜色选择环（中间） │  │  ← 全色域彩色环，拖动选择点选颜色

&#x20;      │  │  ╭───────────────╮  │  │

&#x20;      │  │  │ 第3层：当前颜色圆 │  │  │  ← 实心圆，显示当前氛围灯颜色

&#x20;      │  │  ╰───────────────╯  │  │

&#x20;      │  ╰─────────────────────╯  │

&#x20;      ╰─────────────────────────╯
```

**第 1 层：亮度调节环（最外圈）**



| 项目   | 说明                                          |
| ---- | ------------------------------------------- |
| 外观   | 由一圈细小竖线（刻度 / 睫毛状）组成的圆环，环绕在色盘最外侧             |
| 交互   | 沿圆环拖动手指，顺时针或逆时针方向均可调节亮度                     |
| 循环特性 | 亮度可循环调节，顺时针持续拖动可从最亮→最暗→最亮循环，逆时针同理           |
| 范围   | 0%-100%（0% 为熄灭，100% 为最亮）                    |
| 反馈 1 | 拖动时亮度即时变化并下发到硬件，竖线高亮位置指示当前亮度                |
| 反馈 2 | 拖动时触发第 3 层内芯圆的亮度指示器（灯泡图标 + 百分比数字），详见第 3 层说明 |
| 下限保护 | 亮度降到 0% 时继续同方向拖动将循环到 100%，无阻断提示             |

**第 2 层：颜色选择环（中间）**



| 项目     | 说明                                      |
| ------ | --------------------------------------- |
| 外观     | HSV 全色域彩色圆环，色相沿圆周 0°-360° 分布，饱和度沿半径方向渐变 |
| 交互     | 拖动环上的选择点（小圆点），实时切换颜色                    |
| 反馈     | 选择点位置即时跟随手指，颜色即时下发到硬件，最内层颜色圆同步更新        |
| 与预设色联动 | 点击下方预设颜色块时，选择点自动移动到对应颜色位置               |

**第 3 层：当前颜色显示圆（最内层）**

第 3 层具有**两种显示状态**，根据用户操作自动切换：

**状态 A：默认状态（非亮度调节时）**



| 项目   | 说明                              |
| ---- | ------------------------------- |
| 外观   | 实心圆，纯颜色填充，无任何叠加图标或文字            |
| 填充色  | 当前氛围灯的颜色（与色盘选择点、预设色选择同步）        |
| 功能   | 实时显示当前氛围灯的颜色，作为颜色选择的视觉反馈        |
| 触发条件 | 未触摸外圈亮度调节环，或亮度调节结束后约 500ms 自动恢复 |

**状态 B：亮度调节指示状态（拖动外圈亮度环时）**

当用户手指触摸并拖动外圈亮度调节环时，第 3 层内芯圆从状态 A 切换为状态 B，叠加亮度指示器：



```
&#x20;         ╭─────────────╮

&#x20;        │   「 75% 」    │  ← 亮度百分比数字（内芯圆正上方）

&#x20;        │               │

&#x20;        │     💡         │  ← 灯泡图标（内芯圆中心）

&#x20;        │   (随亮度变化)  │

&#x20;        ╰─────────────╯
```



| 项目     | 说明                                                                 |
| ------ | ------------------------------------------------------------------ |
| 背景     | 内芯圆仍填充当前氛围灯颜色（作为底色），亮度指示器叠加在其上                                     |
| 灯泡图标   | 位于内芯圆中心，白色灯泡形状（💡），尺寸 = 内芯圆直径 × 0.4                                |
| 灯泡亮度变化 | 灯泡图标随当前亮度值实时变化：亮度 100% 时灯泡最亮（纯白色，带光晕）；亮度 0% 时灯泡最暗（深灰色，无光晕）；中间值线性过渡 |
| 光晕效果   | 亮度 > 50% 时灯泡周围显示白色光晕，光晕半径随亮度增大而增大                                  |
| 百分比数字  | 位于内芯圆正上方，距内芯圆顶部约 8dp，格式为「{数值}%」，如「75%」                             |
| 数字样式   | 14sp，粗体，白色，带半透明黑色描边（确保在任意颜色底色上可读）                                  |
| 数字范围   | 0% - 100%，整数显示，实时跟随亮度值更新                                           |
| 切换动画   | 状态 A→状态 B：灯泡图标和数字淡入（200ms）；状态 B→状态 A：淡出（300ms）                     |
| 触发条件   | 手指触摸外圈亮度环并开始拖动时立即切换为状态 B                                           |
| 恢复条件   | 手指离开屏幕后，延迟 500ms 自动从状态 B 恢复为状态 A                                   |

**预设颜色块：**



| 项目  | 说明                             |
| --- | ------------------------------ |
| 数量  | 8 个（2 行 × 4 列，或 1 行 × 8 列滚动）   |
| 形状  | 圆形色块                           |
| 交互  | 点击即切换为该颜色，色盘选择点同步移动，最内层颜色圆同步更新 |
| 颜色值 | 预设 8 种常用色（具体值见 3.3 节）          |

**RGB 实时显示：**



| 项目 | 说明                            |
| -- | ----------------------------- |
| 位置 | 预设颜色块左侧                       |
| 格式 | 三行显示：R \[值] / G \[值] / B \[值] |
| 范围 | 0-255                         |
| 更新 | 颜色变化时实时更新                     |

#### 2.3.4 速度调节



| 项目     | 说明               |
| ------ | ---------------- |
| 控件     | 横向滑块，位于色盘下方      |
| 左端     | 标注 "慢"           |
| 右端     | 标注 "快"           |
| 交互     | 拖动滑块圆点，实时调节速度    |
| 范围     | 0-100（映射到硬件速度等级） |
| 默认值    | 50（中间位置）         |
| 生效场景   | 对呼吸、跳变等动态效果生效    |
| 单色常亮模式 | 速度调节不影响显示，但保留设置值 |

#### 2.3.5 颜色模式切换

**模式定义：**



| 模式 ID | 模式名称 | 说明              |
| ----- | ---- | --------------- |
| 0x00  | 单色   | 固定一种颜色常亮，色盘选择生效 |
| 0x01  | 十二色  | 12 种预设颜色自动循环切换  |
| 0x02  | 七色   | 7 种彩虹色自动循环切换    |

**交互方式：**



* 主控制页底部中间按钮显示当前模式（默认 "单色"）

* 点击按钮 → 弹出颜色模式选择弹窗

* 弹窗选项：单色 / 十二色 / 七色

* 选择后点击 "确定" 生效

* 十二色 / 七色模式下，色盘选择禁用（置灰），颜色由硬件自动循环

* 速度滑块控制循环切换速度

#### 2.3.6 动态效果切换

**效果定义：**



| 效果 ID | 效果名称 | 说明                |
| ----- | ---- | ----------------- |
| 0x00  | 关闭   | 无动态效果，常亮          |
| 0x01  | 呼吸   | 亮度明暗渐变，模拟呼吸节奏     |
| 0x02  | 跳变   | 颜色 / 亮度直接切换，无过渡动画 |

**交互方式：**



* 主控制页底部右侧按钮显示当前效果（默认 "呼吸"）

* 点击按钮 → 弹出动态效果选择弹窗

* 弹窗选项：关闭 / 呼吸 / 跳变

* 选择后点击 "确定" 生效

* 速度滑块对呼吸、跳变效果生效，控制变化速率

### 2.4 辅助功能模块

#### 2.4.1 帮助中心



* 从主控制页右上角三点菜单进入

* 内容包含：


  * 蓝牙连接教程（图文）

  * 灯光控制操作指南

  * 常见问题 FAQ

  * 联系客服方式

* 支持 WebView 加载在线帮助文档，或本地静态页面

#### 2.4.2 关于我们



* 从主控制页右上角三点菜单进入

* 内容包含：


  * APP 名称和版本号

  * 公司 / 开发者信息

  * 隐私政策链接

  * 用户协议链接

  * 官方网站 / 公众号二维码

### 2.5 系统功能

#### 2.5.1 权限管理



| 权限   | 用途                  | 申请时机        |
| ---- | ------------------- | ----------- |
| 蓝牙扫描 | 搜索附近氛围灯设备           | 进入蓝牙设置页时    |
| 蓝牙连接 | 连接氛围灯设备             | 点击设备连接时     |
| 位置信息 | Android 12 以下蓝牙扫描必需 | 进入蓝牙设置页时    |
| 附近设备 | Android 12+ 蓝牙权限    | 进入蓝牙设置页时    |
| 后台运行 | 保持蓝牙连接              | 首次启动时引导用户设置 |

#### 2.5.2 后台保活



* 启动前台服务（Foreground Service）保持蓝牙连接

* 服务栏显示 "汽车氛围灯已连接" 通知

* 锁屏后保持蓝牙连接和灯光控制状态

#### 2.5.3 错误处理与提示



| 错误场景    | 提示方式           | 处理逻辑                 |
| ------- | -------------- | -------------------- |
| 蓝牙未开启   | 系统弹窗 + APP 内提示 | 引导用户开启蓝牙             |
| 未找到设备   | 设备列表空状态提示      | 显示 "未找到设备，请检查设备是否通电" |
| 连接失败    | Toast 提示       | 保留设备列表，允许重试          |
| 连接断开    | Toast + 自动重连   | 自动重连 3 次，失败后提示       |
| 指令发送失败  | 静默重试 1 次       | 重试失败后提示 "控制指令发送失败"   |
| 蓝牙硬件不支持 | 启动时检测          | 提示 "当前设备不支持蓝牙低功耗"    |



***

## 3. 界面设计规范

### 3.1 整体视觉风格



| 项目        | 规范                                |
| --------- | --------------------------------- |
| 主题        | 深色科技风                             |
| 背景色       | #1A1A1A（深灰黑）或 #000000（纯黑）         |
| 主色调       | #00E5FF（青色 / 蓝绿色高亮）               |
| 文字主色      | #FFFFFF（白色）                       |
| 文字次色      | #999999（浅灰）                       |
| 卡片 / 弹窗背景 | #2A2A2A（深灰）                       |
| 按钮高亮      | #00E5FF                           |
| 圆角        | 按钮 / 弹窗 8dp-12dp                  |
| 字体        | 系统默认无衬线字体（Android: Roboto / 思源黑体） |

### 3.2 页面结构

#### 3.2.1 主控制页（MainActivity）

**布局结构（自上而下）：**



```
┌─────────────────────────────────────────┐

│ 状态栏（系统）                              │

├─────────────────────────────────────────┤

│ \[头像]  欢迎您！{用户昵称}    \[⋮] \[⚙]   │  ← 顶部栏（高度 56dp）

├─────────────────────────────────────────┤

│                                         │

│      ╭─────────────────────╮            │

│      │  第1层：亮度调节环     │            │  ← 色盘区域（三层同心圆环）

│      │  ╭───────────────╮  │            │     直径 = 屏幕宽度 × 0.75

│      │  │ 第2层：颜色选择环 │  │            │

│      │  │  ╭───────────╮  │  │            │

│      │  │  │第3层:当前色│  │  │            │

│      │  │  ╰───────────╯  │  │            │

│      │  ╰───────────────╯  │            │

│      ╰─────────────────────╯            │

│                                         │

├─────────────────────────────────────────┤

│  慢  ───────────●───────────  快        │  ← 速度滑块（高度 48dp）

├─────────────────────────────────────────┤

│  ┌─────────┬──────────┬─────────┐      │

│  │  {分区}  │  {颜色模式} │ {动态效果} │      │  ← 三参数按钮（高度 48dp）

│  └─────────┴──────────┴─────────┘      │

├─────────────────────────────────────────┤

│ ┌───┐  \[●]\[●]\[●]\[●]                    │

│ │R  │                                    │

│ │G  │  \[●]\[●]\[●]\[●]                    │  ← 预设颜色 + RGB 显示

│ │B  │                                    │

│ └───┘                                    │

├─────────────────────────────────────────┤

│ 底部导航栏（系统）                         │

└─────────────────────────────────────────┘
```

**各区域详细规范：**

**顶部栏：**



* 高度：56dp

* 左侧：圆形用户头像（直径 36dp）+ "欢迎您！{昵称}" 文字（14sp，白色）

* 右侧：三点菜单图标（24dp）+ 设置图标（24dp，滑块样式）

* 点击三点菜单 → 从右侧滑出侧边菜单

* 点击设置图标 → 预留入口（V1.0 可暂不实现，或跳转高级设置页）

**色盘区域（三层同心圆环组件）：**



* 整体直径：屏幕宽度 × 0.75（最小 240dp，最大 360dp）

* 色盘居中显示

* **第 1 层：亮度调节环（最外圈）**


  * 由一圈细小竖线（刻度 / 睫毛状）组成，环绕在色盘最外侧

  * 竖线数量：约 60-120 根，均匀分布，长度 6dp-10dp

  * 竖线颜色：未选中部分为深灰（#444444），当前亮度位置及之前为青色高亮（#00E5FF）

  * 交互：沿圆环拖动手指，顺时针 / 逆时针均可调节亮度，可循环

  * 触摸热区：最外圈向外扩展 16dp，确保易触摸

* **第 2 层：颜色选择环（中间）**


  * HSV 全色域彩色圆环，宽度 = 整体直径 × 0.25

  * 色相沿圆周 0°-360° 分布，饱和度沿半径方向从内到外渐变

  * 选择点：白色小圆点（直径 16dp），带黑色描边，指示当前选中颜色位置

  * 交互：拖动选择点实时切换颜色

* **第 3 层：当前颜色显示圆（最内层）**


  * 直径 = 整体直径 × 0.35

  * 具有两种显示状态，根据操作自动切换：

  * **状态 A（默认）**：实心圆，纯颜色填充当前氛围灯颜色，无叠加图标和文字

  * **状态 B（亮度调节中）**：


    * 底色仍为当前氛围灯颜色

    * 中心叠加白色灯泡图标（💡），尺寸 = 内芯圆直径 × 0.4

    * 灯泡图标亮度随当前亮度值实时变化：100% 为纯白色带光晕，0% 为深灰色无光晕，中间线性过渡

    * 亮度 > 50% 时灯泡周围显示白色光晕，光晕半径 = (亮度 - 50) / 50 × 12dp

    * 内芯圆正上方显示亮度百分比数字，距圆顶 8dp

    * 数字格式：「{0-100}%」，14sp 粗体白色，带 1dp 半透明黑色描边

    * 状态切换动画：A→B 淡入 200ms，B→A 淡出 300ms

    * 手指离开外圈后延迟 500ms 自动恢复状态 A

**速度滑块：**



* 高度：48dp（含触摸热区）

* 左右边距：32dp

* 左端文字 "慢"：12sp，浅灰色，左对齐

* 右端文字 "快"：12sp，浅灰色，右对齐

* 滑块轨道：高度 4dp，圆角 2dp，已选部分青色，未选部分深灰

* 滑块圆点：直径 20dp，白色，带阴影

**三参数按钮：**



* 高度：48dp

* 左右边距：16dp

* 三个按钮等宽，间距 8dp

* 按钮背景：#2A2A2A，圆角 8dp

* 按钮文字：14sp，白色，居中

* 点击效果：背景变深，有波纹反馈

**预设颜色 + RGB 区域：**



* 高度：约 96dp

* 左侧 RGB 显示区：宽度 80dp


  * R/G/B 三行，每行 24dp 高

  * 标签（R/G/B）：12sp，白色，左对齐

  * 数值：14sp，白色，右对齐

  * 背景：#2A2A2A，圆角 8dp

* 右侧预设颜色区：


  * 2 行 × 4 列，共 8 个圆形色块

  * 色块直径：28dp

  * 色块间距：水平 16dp，垂直 12dp

  * 当前选中色块：外圈 2dp 青色描边

**预设颜色值（建议）：**



| 序号 | 颜色名 | HEX     | RGB           |
| -- | --- | ------- | ------------- |
| 1  | 黄色  | #FFD700 | (255, 215, 0) |
| 2  | 橙色  | #FF8C00 | (255, 140, 0) |
| 3  | 红色  | #FF0000 | (255, 0, 0)   |
| 4  | 蓝色  | #0000FF | (0, 0, 255)   |
| 5  | 玫红  | #FF0066 | (255, 0, 102) |
| 6  | 深蓝  | #00008B | (0, 0, 139)   |
| 7  | 绿色  | #00FF00 | (0, 255, 0)   |
| 8  | 浅绿  | #7FFF00 | (127, 255, 0) |

#### 3.2.2 蓝牙设置页（BluetoothActivity）



```
┌─────────────────────────────────────────┐

│ ← 蓝牙设置                    \[蓝牙状态] │  ← 顶部栏（56dp）

├─────────────────────────────────────────┤

│                                         │

│            ⟳ 点击重新搜索                 │  ← 搜索按钮（居中，带刷新图标）

│                                         │

│  ┌───────────────────────────────────┐  │

│  │  LED-2202080                \[●]   │  │  ← 设备列表项（高度 56dp）

│  └───────────────────────────────────┘  │

│                                         │

│  ┌───────────────────────────────────┐  │

│  │  LED-XXXXXXXX                \[○]   │  │  ← 更多设备...

│  └───────────────────────────────────┘  │

│                                         │

│         \[ 蓝牙连接成功 ] (Toast)         │

└─────────────────────────────────────────┘
```

**规范：**



* 顶部栏：返回箭头 + "蓝牙设置" 标题 + 蓝牙状态图标（开 / 关）

* 搜索按钮：居中，刷新图标 + "点击重新搜索" 文字，16sp

* 搜索中：按钮变为加载动画 + "搜索中..."

* 设备列表项：


  * 高度：56dp

  * 左侧：设备名称（16sp，白色）

  * 右侧：连接状态圆点（直径 12dp，绿色 = 已连接，灰色 = 未连接）

  * 已连接项：背景色 #1A3A3A（微青色底）

  * 点击未连接项：发起连接

  * 点击已连接项：弹出断开确认对话框

* 空状态：设备列表为空时显示 "未找到设备，请检查设备是否通电并开启蓝牙"

#### 3.2.3 侧边菜单（NavigationDrawer）



```
┌──────────────┐

│  \[头像]       │

│  欢乐马       │  ← 用户信息区

├──────────────┤

│  蓝牙设置  >  │

│  帮助中心  >  │  ← 菜单项（高度 48dp）

│  关于我们  >  │

├──────────────┤

│  版本 V1.0.0  │  ← 底部版本号

└──────────────┘
```



* 宽度：屏幕宽度 × 0.75（最大 320dp）

* 从右侧滑出（与小程序一致）

* 背景：#1A1A1A

* 菜单项：图标 + 文字 + 右箭头，16sp 白色

* 点击菜单项：跳转对应页面，关闭菜单

#### 3.2.4 通用选择弹窗（BottomSheetDialog）

三类弹窗（位置 / 颜色模式 / 动态效果）统一使用底部弹出式选择器：



```
┌─────────────────────────────────────────┐

│  ┌───────────────────────────────────┐  │

│  │  ○  全部                            │  │

│  │  ○  前部                            │  │

│  │  ●  车门                            │  │  ← 选项列表（可滚动）

│  │  ○  脚部                            │  │     每项高度 48dp

│  │  ○  顶部                            │  │     选中项：青色圆点 + 文字

│  │  ○  车门一区                         │  │     未选中：灰色圆点 + 文字

│  │  ○  车门二区                         │  │

│  ├───────────────────────────────────┤  │

│  │  \[ 取消 ]          \[ 确定 ]        │  │  ← 底部按钮（高度 48dp）

│  └───────────────────────────────────┘  │

└─────────────────────────────────────────┘
```

**规范：**



* 弹出方式：从底部滑入，背景半透明遮罩 #000000 60%

* 弹窗背景：#2A2A2A，顶部圆角 16dp

* 选项列表：单选模式，每项高度 48dp，左右边距 24dp

* 选中指示：左侧圆形单选框（直径 20dp），选中时填充青色

* 选项文字：16sp，选中白色，未选中浅灰

* 底部按钮：


  * "取消"：左对齐，16sp，浅灰色

  * "确定"：右对齐，16sp，青色

  * 按钮区域高度 56dp，顶部 1dp 分割线 #333333

* 点击遮罩或 "取消"：关闭弹窗，不生效

* 点击 "确定"：生效选择，关闭弹窗



***

## 4. 交互流程设计

### 4.1 首次使用流程



```
APP 启动

&#x20; │

&#x20; ├─ 检测蓝牙权限

&#x20; │    ├─ 未授权 → 申请蓝牙/位置权限

&#x20; │    │              ├─ 授权 → 继续

&#x20; │    │              └─ 拒绝 → 提示"需要蓝牙权限才能使用"，退出或停留

&#x20; │    └─ 已授权 → 继续

&#x20; │

&#x20; ├─ 检测蓝牙开关

&#x20; │    ├─ 未开启 → 弹出系统蓝牙开启请求

&#x20; │    │              ├─ 开启 → 继续

&#x20; │    │              └─ 拒绝 → 提示"请开启蓝牙"

&#x20; │    └─ 已开启 → 继续

&#x20; │

&#x20; ├─ 检查已保存设备

&#x20; │    ├─ 有保存设备 → 自动尝试连接

&#x20; │    │              ├─ 连接成功 → 进入主控制页

&#x20; │    │              └─ 连接失败 → 进入主控制页，提示"未连接"

&#x20; │    └─ 无保存设备 → 进入主控制页，提示"请先连接蓝牙设备"

&#x20; │

&#x20; └─ 进入主控制页
```

### 4.2 蓝牙连接流程



```
用户点击右上角菜单 → 选择"蓝牙设置"

&#x20; │

&#x20; └─ 进入蓝牙设置页

&#x20;      │

&#x20;      ├─ 自动开始扫描（10秒超时）

&#x20;      │    └─ 显示设备列表

&#x20;      │

&#x20;      ├─ 用户点击设备

&#x20;      │    ├─ 未连接 → 显示"连接中"

&#x20;      │    │           ├─ 成功 → Toast"蓝牙连接成功"，设备变绿

&#x20;      │    │           └─ 失败 → Toast"连接失败"，设备保持灰色

&#x20;      │    └─ 已连接 → 弹出"是否断开连接？"

&#x20;      │                 ├─ 确定 → 断开，设备变灰

&#x20;      │                 └─ 取消 → 无操作

&#x20;      │

&#x20;      └─ 用户点击"点击重新搜索" → 重新扫描
```

### 4.3 灯光控制流程



```
主控制页（已连接状态）

&#x20; │

&#x20; ├─ 颜色控制

&#x20; │    ├─ 拖动色盘 → 实时更新颜色 → 下发蓝牙指令 → RGB值更新

&#x20; │    └─ 点击预设色 → 色盘点位移动 → 下发蓝牙指令 → RGB值更新

&#x20; │

&#x20; ├─ 亮度控制

&#x20; │    ├─ 手指触摸外圈竖线环 → 内芯圆切换为亮度指示状态（灯泡图标+百分比数字淡入）

&#x20; │    ├─ 沿圆环拖动（顺时针/逆时针均可，可循环）→ 实时更新亮度值 → 下发蓝牙指令

&#x20; │    │    ├─ 外圈竖线高亮位置随亮度移动

&#x20; │    │    ├─ 内芯圆灯泡图标亮度随亮度值变化（亮→白/暗→灰）

&#x20; │    │    └─ 内芯圆上方百分比数字实时更新（0%-100%）

&#x20; │    └─ 手指离开屏幕 → 延迟 500ms → 内芯圆恢复默认颜色显示状态（指示器淡出）

&#x20; │

&#x20; ├─ 速度控制

&#x20; │    └─ 拖动滑块 → 实时更新速度值 → 下发蓝牙指令

&#x20; │

&#x20; ├─ 分区切换

&#x20; │    └─ 点击分区按钮 → 弹出位置选择弹窗

&#x20; │         ├─ 选择分区 → 确定 → 按钮文字更新 → 加载该分区保存的参数

&#x20; │         └─ 取消 → 关闭弹窗

&#x20; │

&#x20; ├─ 颜色模式切换

&#x20; │    └─ 点击模式按钮 → 弹出颜色模式弹窗

&#x20; │         ├─ 单色 → 确定 → 色盘启用

&#x20; │         ├─ 十二色 → 确定 → 色盘禁用，硬件自动循环

&#x20; │         └─ 七色 → 确定 → 色盘禁用，硬件自动循环

&#x20; │

&#x20; └─ 动态效果切换

&#x20;      └─ 点击效果按钮 → 弹出动态效果弹窗

&#x20;           └─ 关闭/呼吸/跳变 → 确定 → 下发效果指令
```

### 4.4 分区参数记忆逻辑

每个分区独立保存以下参数，切换分区时自动恢复：



```
分区参数存储结构：

{

&#x20; "zone\_0": { "color": \[R,G,B], "brightness": 0-100, "colorMode": 0-2, "effect": 0-4, "speed": 0-100 },

&#x20; "zone\_1": { ... },

&#x20; ...

&#x20; "zone\_6": { ... }

}
```



* APP 本地持久化存储（SharedPreferences）

* 切换分区时，从本地读取该分区参数并更新 UI

* 参数变更时，即时保存到本地并下发蓝牙指令

* "全部" 分区（zone\_0）的操作会同时下发到所有硬件分区，但不覆盖各分区独立保存的参数



***

## 5. 技术架构设计

### 5.1 整体架构



```
┌─────────────────────────────────────────────────┐

│                   UI 层                          │

│  MainActivity / BluetoothActivity / Dialogs     │

│  (主控制页 / 蓝牙设置页 / 各类弹窗)               │

├─────────────────────────────────────────────────┤

│                 ViewModel 层                     │

│  LightControlViewModel / BluetoothViewModel     │

│  (灯光控制状态管理 / 蓝牙连接状态管理)             │

├─────────────────────────────────────────────────┤

│                  业务层                          │

│  ┌──────────────┐  ┌──────────────────────┐    │

│  │ LightController│  │  BluetoothManager     │    │

│  │ (灯光控制逻辑)  │  │  (蓝牙扫描/连接/通信)  │    │

│  └──────────────┘  └──────────────────────┘    │

│  ┌──────────────────────────────────────────┐   │

│  │           ZoneStateStore (分区参数持久化)    │   │

│  └──────────────────────────────────────────┘   │

├─────────────────────────────────────────────────┤

│                  平台层                          │

│         Android Bluetooth LE API                 │

│              (系统蓝牙API)                        │

└─────────────────────────────────────────────────┘
```

### 5.2 技术选型



| 层级     | 技术选型                          | 说明                             |
| ------ | ----------------------------- | ------------------------------ |
| 开发语言   | Kotlin                        | Android 官方推荐语言                 |
| UI 框架  | Jetpack Compose 或 XML Layout  | Compose 优先，XML 备选              |
| 架构模式   | MVVM                          | ViewModel + LiveData/StateFlow |
| 蓝牙库    | 原生 BluetoothLe API 或 FastBLE  | 原生 API 更可控，FastBLE 更简洁         |
| 异步处理   | Kotlin Coroutines + Flow      | 蓝牙操作异步化                        |
| 数据持久化  | SharedPreferences / DataStore | 保存分区参数、已连接设备                   |
| 依赖注入   | Hilt                          | 可选，简化模块管理                      |
| 最低 SDK | API 26 (Android 8.0)          | 覆盖主流设备                         |
| 目标 SDK | API 34 (Android 14)           | 最新稳定版                          |

### 5.3 模块划分



```
app/

├── ui/

│   ├── main/                    # 主控制页

│   │   ├── MainActivity.kt

│   │   ├── MainViewModel.kt

│   │   └── components/          # 色盘、滑块、按钮等自定义组件

│   ├── bluetooth/               # 蓝牙设置页

│   │   ├── BluetoothActivity.kt

│   │   └── BluetoothViewModel.kt

│   ├── help/                    # 帮助中心

│   ├── about/                   # 关于我们

│   └── dialogs/                 # 通用选择弹窗

├── bluetooth/

│   ├── BluetoothManager.kt      # 蓝牙扫描、连接、状态管理

│   ├── LightCommand.kt          # 灯光指令封装

│   └── GattAttributes.kt        # GATT 服务/特征值 UUID 定义

├── light/

│   ├── LightController.kt       # 灯光控制业务逻辑

│   ├── ZoneState.kt             # 分区状态数据类

│   └── ZoneStateStore.kt        # 分区参数持久化

├── data/

│   ├── AppPreferences.kt        # 全局偏好设置

│   └── model/                   # 数据模型定义

└── utils/

&#x20;   ├── ColorUtils.kt             # 颜色转换工具

&#x20;   └── PermissionUtils.kt        # 权限管理工具
```

### 5.4 蓝牙通信架构



```
BluetoothManager（单例）

&#x20; │

&#x20; ├── 扫描管理

&#x20; │    ├── startScan() / stopScan()

&#x20; │    ├── scanCallback → 设备列表 Flow

&#x20; │    ├── 扫描过滤器（名称前缀 "LED-"，严格前缀匹配）

&#x20; │    │    ├── 第一层：硬件 ScanFilter.setDeviceName("LED-") 前缀过滤

&#x20; │    │    └── 第二层：软件回调中二次校验 device.name?.startsWith("LED-") == true

&#x20; │    └── 过滤规则：仅展示名称以 "LED-" 开头的设备，名称为空/不符/小写均丢弃

&#x20; │

&#x20; ├── 连接管理

&#x20; │    ├── connect(deviceAddress) / disconnect()

&#x20; │    ├── connectionState → StateFlow\<ConnectionState>

&#x20; │    ├── 自动重连逻辑

&#x20; │    └── 超时控制

&#x20; │

&#x20; └── 数据通信

&#x20;      ├── writeCommand(command: LightCommand)

&#x20;      ├── 指令队列（串行发送，避免丢包）

&#x20;      ├── 发送重试机制（失败重试 1 次）

&#x20;      └── 数据回调（onCharacteristicChanged）
```



***

## 6. 蓝牙通信协议

### 6.1 GATT 服务定义

> 以下 UUID 为推测值，实际需根据硬件固件确认。



| 类型                    | UUID                                   | 说明                               |
| --------------------- | -------------------------------------- | -------------------------------- |
| Service               | `0000FFF0-0000-1000-8000-00805F9B34FB` | 自定义服务（常见氛围灯方案）                   |
| Write Characteristic  | `0000FFF1-0000-1000-8000-00805F9B34FB` | 指令写入特征值（Write/Write No Response） |
| Notify Characteristic | `0000FFF2-0000-1000-8000-00805F9B34FB` | 状态通知特征值（Notify/Indicate）         |

### 6.2 指令帧格式

**通用指令格式：**



```
字节位置    字段名        长度    说明

─────────────────────────────────────────────

Byte 0      帧头          1B      固定 0xAA

Byte 1      指令类型      1B      见 6.3 指令类型表

Byte 2      数据长度      1B      后续数据区字节数（N）

Byte 3..N+2 数据区        N B     指令参数

Byte N+3    校验和        1B      帧头到数据区所有字节的累加和，取低 8 位

Byte N+4    帧尾          1B      固定 0x55
```

**示例：设置全部区域为红色（R=255, G=0, B=0）**



```
AA 01 05 00 FF 00 00 ?? 55

│  │  │  │  │  │  │  │  └─ 帧尾

│  │  │  │  │  │  │  └──── 校验和

│  │  │  │  │  │  └─────── B=0

│  │  │  │  │  └────────── G=0

│  │  │  │  └───────────── R=255

│  │  │  └──────────────── 分区=全部(0x00)

│  │  └─────────────────── 数据长度=5

│  └────────────────────── 指令类型=设置颜色(0x01)

└───────────────────────── 帧头
```

### 6.3 指令类型表



| 指令类型   | 码值   | 数据区内容             | 说明                    |
| ------ | ---- | ----------------- | --------------------- |
| 设置颜色   | 0x01 | \[分区]\[R]\[G]\[B] | 设置指定分区的颜色（单色模式）       |
| 设置亮度   | 0x02 | \[分区]\[亮度]        | 亮度 0-255（或 0-100，需确认） |
| 设置速度   | 0x03 | \[速度]             | 动态效果速度 0-255          |
| 设置颜色模式 | 0x04 | \[分区]\[模式]        | 0 = 单色，1 = 十二色，2 = 七色 |
| 设置动态效果 | 0x05 | \[分区]\[效果]        | 0 = 关闭，1 = 呼吸，2 = 跳变  |
| 查询状态   | 0x10 | \[分区]             | 查询指定分区当前状态（可选）        |
| 设备信息   | 0xF0 | -                 | 查询设备固件版本、MAC 等（可选）    |

### 6.4 分区编码



| 分区名称 | 编码值  |
| ---- | ---- |
| 全部   | 0x00 |
| 前部   | 0x01 |
| 车门   | 0x02 |
| 脚部   | 0x03 |
| 顶部   | 0x04 |
| 车门一区 | 0x05 |
| 车门二区 | 0x06 |

### 6.5 通信注意事项



1. **写入方式**：使用 Write No Response（无需应答），提高响应速度

2. **指令间隔**：连续指令之间至少间隔 20ms，避免蓝牙缓冲区溢出

3. **MTU**：连接后请求 MTU 协商（建议 247 字节），单条指令不超过 20 字节无需分片

4. **连接间隔**：请求较小的连接间隔（15ms-30ms），降低控制延迟

5. **指令队列**：APP 端维护指令发送队列，串行发送，前一条发送完成后再发下一条

6. **丢包处理**：关键指令（连接 / 模式切换）发送后等待硬件 Notify 确认，超时重发



***

## 7. 数据模型定义

### 7.1 分区状态（ZoneState）



```
data class ZoneState(

&#x20;   val zoneId: Int,           // 分区 ID 0-6

&#x20;   val color: Int,             // 当前颜色（ARGB 格式，0xFFRRGGBB）

&#x20;   val brightness: Int,        // 亮度 0-100

&#x20;   val colorMode: ColorMode,   // 颜色模式

&#x20;   val effect: LightEffect,    // 动态效果

&#x20;   val speed: Int              // 速度 0-100

)

enum class ColorMode(val value: Int, val displayName: String) {

&#x20;   SINGLE(0, "单色"),

&#x20;   TWELVE\_COLOR(1, "十二色"),

&#x20;   SEVEN\_COLOR(2, "七色")

}

enum class LightEffect(val value: Int, val displayName: String) {

&#x20;   OFF(0, "关闭"),

&#x20;   BREATHING(1, "呼吸"),

&#x20;   JUMP(2, "跳变")

}
```

### 7.2 蓝牙设备（BluetoothDeviceInfo）



```
data class BluetoothDeviceInfo(

&#x20;   val name: String,           // 设备名称

&#x20;   val address: String,        // MAC 地址

&#x20;   val rssi: Int,              // 信号强度 dBm

&#x20;   val isConnected: Boolean    // 是否已连接

)
```

### 7.3 连接状态（ConnectionState）



```
sealed class ConnectionState {

&#x20;   object Disconnected : ConnectionState()

&#x20;   object Connecting : ConnectionState()

&#x20;   object Connected : ConnectionState()

&#x20;   data class Failed(val reason: String) : ConnectionState()

}
```

### 7.4 灯光指令（LightCommand）



```
sealed class LightCommand {

&#x20;   data class SetColor(val zone: Int, val r: Int, val g: Int, val b: Int) : LightCommand()

&#x20;   data class SetBrightness(val zone: Int, val brightness: Int) : LightCommand()

&#x20;   data class SetSpeed(val speed: Int) : LightCommand()

&#x20;   data class SetColorMode(val zone: Int, val mode: Int) : LightCommand()

&#x20;   data class SetEffect(val zone: Int, val effect: Int) : LightCommand()

}
```



***

## 8. 开发计划与里程碑

### 8.1 总体周期



| 阶段      | 周期      | 交付物                   |
| ------- | ------- | --------------------- |
| 需求确认与设计 | 1 周     | 开发说明书终稿、UI 设计稿、蓝牙协议确认 |
| 基础框架搭建  | 1 周     | 项目骨架、导航、基础 UI 组件      |
| 蓝牙模块开发  | 2 周     | 扫描、连接、通信、自动重连         |
| 灯光控制模块  | 2 周     | 色盘、亮度、速度、模式、效果、分区     |
| 辅助功能与优化 | 1 周     | 帮助中心、关于我们、权限处理、后台保活   |
| 测试与修复   | 2 周     | 功能测试、兼容性测试、Bug 修复     |
| **合计**  | **9 周** |                       |

### 8.2 里程碑

**M1（第 2 周末）：基础框架完成**



* 项目可编译运行

* 主控制页静态 UI 完成（色盘、滑块、按钮等组件）

* 蓝牙设置页静态 UI 完成

* 页面导航正常

**M2（第 4 周末）：蓝牙通信完成**



* 蓝牙扫描、设备列表展示

* 设备连接 / 断开、状态监听

* 指令封装与发送

* 自动重连机制

**M3（第 6 周末）：核心灯光控制完成**



* 色盘选色、预设色、RGB 显示

* 亮度调节、速度调节

* 分区切换与参数记忆

* 颜色模式切换

* 动态效果切换

**M4（第 7 周末）：辅助功能完成**



* 帮助中心页面

* 关于我们页面

* 权限管理与错误提示

* 后台保活与前台服务

**M5（第 9 周末）：发布就绪**



* 全功能测试通过

* 多机型兼容性测试

* 性能优化（内存、电量）

* 签名打包 Release 版本

### 8.3 任务分解（WBS）



```
1\. 需求与设计

&#x20;  1.1 开发说明书评审与确认

&#x20;  1.2 UI 设计稿（Figma/Sketch）

&#x20;  1.3 蓝牙协议与硬件联调确认

&#x20;  1.4 技术方案评审

2\. 基础框架

&#x20;  2.1 创建 Android 项目，配置 Gradle

&#x20;  2.2 搭建 MVVM 架构基础

&#x20;  2.3 实现主题与样式（深色科技风）

&#x20;  2.4 实现页面导航框架

&#x20;  2.5 自定义组件：环形色盘

&#x20;  2.6 自定义组件：速度滑块

&#x20;  2.7 自定义组件：通用选择弹窗

3\. 蓝牙模块

&#x20;  3.1 蓝牙权限管理

&#x20;  3.2 蓝牙开关检测与请求

&#x20;  3.3 BLE 设备扫描（名称过滤）

&#x20;  3.4 设备列表 UI 与状态显示

&#x20;  3.5 GATT 连接管理

&#x20;  3.6 连接状态监听与回调

&#x20;  3.7 指令队列与发送

&#x20;  3.8 自动重连机制

&#x20;  3.9 后台保活服务

4\. 灯光控制

&#x20;  4.1 主控制页 UI 组装

&#x20;  4.2 色盘拖动选色（HSV 转换）

&#x20;  4.3 预设颜色点击切换

&#x20;  4.4 RGB 数值实时显示

&#x20;  4.5 亮度调节（外圈环形拖动 + 内芯圆亮度指示器）

&#x20;  4.6 速度滑块调节

&#x20;  4.7 分区选择弹窗与切换

&#x20;  4.8 分区参数本地存储与恢复

&#x20;  4.9 颜色模式切换弹窗

&#x20;  4.10 动态效果切换弹窗

&#x20;  4.11 指令下发与状态同步

5\. 辅助功能

&#x20;  5.1 侧边菜单实现

&#x20;  5.2 帮助中心页面（WebView）

&#x20;  5.3 关于我们页面

&#x20;  5.4 全局错误处理与提示

&#x20;  5.5 首次使用引导

6\. 测试与优化

&#x20;  6.1 单元测试（蓝牙指令、颜色转换）

&#x20;  6.2 功能测试用例执行

&#x20;  6.3 多机型兼容性测试

&#x20;  6.4 蓝牙连接稳定性测试

&#x20;  6.5 内存与电量优化

&#x20;  6.6 Bug 修复与回归测试

&#x20;  6.7 Release 签名打包
```



***

## 9. 验收标准

### 9.1 功能验收



| 编号  | 验收项    | 标准                                                                                          |
| --- | ------ | ------------------------------------------------------------------------------------------- |
| F01 | 蓝牙扫描   | 能扫描到名称以 "LED-" 开头的设备，10 秒内显示；严格前缀过滤，名称为空、不以 "LED-" 开头、小写 "led-" 开头的设备均不展示在列表中               |
| F02 | 蓝牙连接   | 点击设备后 5 秒内连接成功，显示 "蓝牙连接成功"                                                                  |
| F03 | 蓝牙断开   | 点击已连接设备，确认后可正常断开                                                                            |
| F04 | 自动重连   | 连接意外断开后自动重连，最多 3 次                                                                          |
| F05 | 色盘选色   | 拖动色盘，灯光颜色实时变化，延迟 < 200ms                                                                    |
| F06 | 预设色    | 点击 8 个预设色，灯光正确切换为对应颜色                                                                       |
| F07 | RGB 显示 | 颜色变化时，R/G/B 数值实时正确更新                                                                        |
| F08 | 亮度调节   | 沿色盘最外圈竖线环拖动，灯光亮度实时连续变化，支持顺时针 / 逆时针循环调节；拖动时内芯圆显示灯泡图标 + 亮度百分比数字，灯泡亮度随数值变化，停止拖动 500ms 后恢复纯颜色显示 |
| F09 | 速度调节   | 拖动速度滑块，动态效果速度实时变化                                                                           |
| F10 | 分区切换   | 7 个分区可独立切换，切换后参数正确恢复                                                                        |
| F11 | 颜色模式   | 单色 / 十二色 / 七色三种模式可正常切换                                                                      |
| F12 | 动态效果   | 关闭 / 呼吸 / 跳变三种效果可正常切换                                                                       |
| F13 | 后台运行   | APP 退到后台后，蓝牙连接保持，灯光控制状态不变                                                                   |
| F14 | 帮助中心   | 可正常打开帮助中心页面                                                                                 |
| F15 | 关于我们   | 可正常打开关于我们页面，显示版本信息                                                                          |

### 9.2 性能验收



| 编号  | 验收项     | 标准                   |
| --- | ------- | -------------------- |
| P01 | 冷启动时间   | < 2 秒（到主控制页可交互）      |
| P02 | 蓝牙连接时间  | < 5 秒（从点击到连接成功）      |
| P03 | 控制响应延迟  | < 200ms（从操作到灯光变化）    |
| P04 | 内存占用    | 前台 < 150MB，后台 < 80MB |
| P05 | 电量消耗    | 持续控制 1 小时耗电 < 15%    |
| P06 | 连续操作稳定性 | 连续操作 30 分钟无崩溃、无断连    |

### 9.3 兼容性验收



| 编号  | 验收项        | 标准                        |
| --- | ---------- | ------------------------- |
| C01 | Android 版本 | Android 8.0 - 14 全版本兼容    |
| C02 | 屏幕尺寸       | 4.7 寸 - 6.8 寸手机正常显示，无布局错乱 |
| C03 | 蓝牙芯片       | 主流厂商蓝牙芯片（高通、联发科、华为麒麟）兼容   |
| C04 | 深色模式       | 全局深色主题，无白底 / 白字不可读问题      |
| C05 | 横竖屏        | 锁定竖屏，横屏时不旋转或自适应           |



***

## 10. 附录

### 10.1 参考资料



* 微信小程序 "汽车氛围灯" 体验分析报告（同目录下）

* Android Bluetooth LE 官方文档：[https://developer.android.com/guide/topics/connectivity/bluetooth](https://developer.android.com/guide/topics/connectivity/bluetooth)

* 常见 BLE 氛围灯控制方案（Nordic nRF51822 / TI CC2541 等）

### 10.2 待确认事项

以下事项需与硬件固件开发人员确认后更新：



| 编号      | 待确认项                           | 影响                                            |
| ------- | ------------------------------ | --------------------------------------------- |
| Q01     | GATT 服务和特征值的实际 UUID            | 蓝牙通信模块                                        |
| Q02     | 指令帧格式是否与 6.2 节一致               | 指令封装                                          |
| Q03     | 亮度 / 速度的实际取值范围（0-100 还是 0-255） | 参数映射                                          |
| Q04     | 十二色 / 七色模式的具体颜色序列              | 模式实现                                          |
| ~~Q05~~ | ~~音乐模式是硬件处理还是 APP 处理~~         | ~~架构设计~~ **【已删除：音乐模式功能已移除】**                  |
| Q06     | 是否支持状态查询指令（读取当前灯光参数）           | 状态同步                                          |
| ~~Q07~~ | ~~设备名称前缀是否固定为 "LED-"~~         | ~~扫描过滤~~ **【已确认：固定以 "LED-" 开头，已更新至 2.2.2 节】** |
| Q08     | 是否支持 OTA 固件升级                  | 后续版本规划                                        |

### 10.3 版本历史



| 版本   | 日期         | 修改人   | 修改内容                                                                                                                                                                                                                                              |
| ---- | ---------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| V1.0 | 2026-09-07 | AI 助手 | 初稿，基于微信小程序体验分析整理                                                                                                                                                                                                                                  |
| V1.1 | 2026-09-07 | AI 助手 | 1. 确认蓝牙设备名固定以 "LED-" 开头并过滤，更新扫描过滤规则；2. 删除动态效果中的 "爆闪" 和 "音乐" 功能，保留关闭 / 呼吸 / 跳变三种；3. 同步删除麦克风权限、音频分析模块、音乐亮度指令、音乐模式开发计划及相关验收标准；4. 开发周期由 10 周调整为 9 周                                                                                                   |
| V1.2 | 2026-09-07 | AI 助手 | 1. 修正色盘组件理解：明确为三层同心圆环结构 —— 最外圈竖线环为亮度调节（环形拖动，顺时针 / 逆时针均可，可循环），中间环为颜色选择，最内层小圆为当前颜色显示；2. 删除原 "色盘左右两侧太阳图标按钮调节亮度" 的错误描述；3. 同步更新 2.3.2 颜色选择、3.2.1 界面布局图与色盘规范、4.3 交互流程、8.3 WBS、9.1 验收标准中亮度调节相关描述                                                         |
| V1.3 | 2026-09-07 | AI 助手 | 1. 新增需求：第 3 层内芯圆增加亮度调节指示器 —— 拖动外圈亮度环时，内芯圆叠加白色灯泡图标 + 亮度百分比数字（0%-100%），灯泡图标亮度随数值实时变化（100% 纯白带光晕 / 0% 深灰无光晕）；2. 内芯圆具有两种状态：默认状态（纯颜色填充）和亮度调节状态（灯泡 + 数字叠加），仅在调节外圈亮度时显示指示器，停止后 500ms 自动恢复；3. 同步更新 2.3.2 第 3 层详细说明、3.2.1 界面设计规范、4.3 交互流程、8.3 WBS、9.1 验收标准 |
| V1.4 | 2026-09-07 | AI 助手 | 1. 补充蓝牙扫描过滤技术实现细节：明确为严格前缀匹配（设备名必须以 "LED-" 开头，非包含匹配），名称为空 / 不符 / 小写均过滤；2. 增加双重过滤机制说明：第一层硬件 ScanFilter.setDeviceName ("LED-") 前缀过滤，第二层软件回调中二次校验 startsWith ("LED-")；3. 增加过滤逻辑 Kotlin 伪代码示例；4. 同步更新 5.4 蓝牙通信架构扫描管理部分、9.1 验收标准 F01                    |



***

*文档结束*